import "../App.css";
import Button from "./Button";

function App() {
    return (
        <div>
            <h1 className="heading">Hi</h1>
            <Button primary />
        </div>
    );
}

export default App;

// ⁡⁢⁣⁣Development : npm start / yarn start -> CSS Internal⁡
// ⁡⁢⁣⁣Production : npm run build / yarn run build -> CSS External⁡
